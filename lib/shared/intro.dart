import 'package:flutter/material.dart';

var introText = Text(
  "Add tasks by clicking the button below. Swipe to delete\nthe task after complete. You can use sort icon to sort you tasks!",
  textAlign: TextAlign.center,
  style: TextStyle(
    fontFamily: 'ProductSans',
    fontSize: 14,
    color: Color(0xff9A9A9A),
  ),
);